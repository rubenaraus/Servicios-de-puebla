import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { PayMultaPage } from './pay-multa';

@NgModule({
  declarations: [
    PayMultaPage,
  ],
  imports: [
    IonicPageModule.forChild(PayMultaPage),
  ],
})
export class PayMultaPageModule {}
